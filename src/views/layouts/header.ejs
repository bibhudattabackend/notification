<nav class="d-flex justify-content-between align-items-center p-3 bg-light">
  <div>
      <a href="/">Home</a>
      <% if (user) { %>
          <a href="/dashboard">Dashboard</a>
          <a href="/logout">Logout</a>
          <div class="position-relative d-inline-block">
              <button id="notification-icon" class="btn btn-outline-primary position-relative">
                  🔔
                  <span id="unread-count" class="badge bg-danger position-absolute top-0 start-100 translate-middle rounded-pill">0</span>
              </button>
              <div id="notification-dropdown" class="dropdown-menu p-2 shadow-sm" style="display: none; width: 300px;">
                  <div class="notification-bar" style="max-height: 300px; overflow-y: auto;">
                      <ul id="notifications" class="list-group"></ul>
                  </div>
                  <div class="mt-2 d-flex justify-content-between">
                      <button id="prev-page" class="btn btn-sm btn-outline-primary">Prev</button>
                      <span id="page-info"></span>
                      <button id="next-page" class="btn btn-sm btn-outline-primary">Next</button>
                  </div>
              </div>
          </div>
      <% } else { %>
          <a href="/login">Login</a>
          <a href="/register">Register</a>
      <% } %>
  </div>
</nav>
